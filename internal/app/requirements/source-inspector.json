{
    "openbindings": "0.2.0",
    "name": "Source Inspector",
    "version": "0.1.0",
    "description": "Interface for inspecting binding source artifacts before an OpenBindings interface exists. A source inspector reports its supported binding specifications and discovers bindable targets that tooling can offer to users.",
    "schemas": {
        "BindingSpecInfo": {
            "type": "object",
            "description": "Information about a supported binding specification.",
            "properties": {
                "bindingSpec": {
                    "type": "string",
                    "description": "Binding specification identifier, exact and opaque (e.g., openbindings.openapi@1, openbindings.mcp@1)."
                },
                "description": {
                    "type": "string",
                    "description": "Human-readable description of this binding specification."
                }
            },
            "required": [
                "bindingSpec"
            ]
        },
        "Source": {
            "type": "object",
            "description": "A binding source (binding-specification identifier plus location or content), as an OpenBindings document carries it. Open to extension fields so a document's source entry forwards verbatim.",
            "properties": {
                "bindingSpec": {
                    "type": "string",
                    "description": "Binding specification identifier governing this source."
                },
                "location": {
                    "type": "string",
                    "description": "Absolute address of the source, per its binding specification (core OBI-D-05)."
                },
                "content": {
                    "description": "Embedded source-artifact representation, per its binding specification."
                },
                "description": {
                    "type": "string",
                    "description": "Human-readable description for this source."
                }
            },
            "required": [
                "bindingSpec"
            ],
            "anyOf": [
                {
                    "required": [
                        "location"
                    ]
                },
                {
                    "required": [
                        "content"
                    ]
                }
            ],
            "additionalProperties": true
        },
        "Operation": {
            "type": "object",
            "description": "Potential OpenBindings operation framing for a bindable target.",
            "properties": {
                "description": {
                    "type": "string"
                },
                "deprecated": {
                    "type": "boolean"
                },
                "tags": {
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                },
                "input": {},
                "output": {}
            },
            "additionalProperties": true
        },
        "BindableTarget": {
            "type": "object",
            "description": "A target admitted by the source's governing binding specification that can be framed as an OpenBindings operation. Any included framing is a sound projection of this same target.",
            "properties": {
                "ref": {
                    "type": "string",
                    "description": "Reference string a conforming binding entry can use under the governing binding specification."
                },
                "operationKey": {
                    "type": "string",
                    "description": "Suggested operation key for this target."
                },
                "operation": {
                    "$ref": "#/schemas/Operation"
                }
            },
            "required": [
                "ref"
            ]
        },
        "InspectSourceInput": {
            "type": "object",
            "properties": {
                "source": {
                    "$ref": "#/schemas/Source"
                }
            },
            "required": [
                "source"
            ],
            "additionalProperties": false
        },
        "SourceInspection": {
            "type": "object",
            "description": "Inspection result for a binding source artifact. Its inventory boundary is the targets admitted by the governing binding specification, not every interaction in the upstream artifact.",
            "properties": {
                "targets": {
                    "type": "array",
                    "items": {
                        "$ref": "#/schemas/BindableTarget"
                    }
                },
                "exhaustive": {
                    "type": "boolean",
                    "description": "True when targets is the complete list of targets admitted by the governing binding specification. False means more admitted targets may exist and requires limitation evidence."
                },
                "limitation": {
                    "$ref": "#/schemas/InspectionLimitation"
                }
            },
            "required": [
                "targets",
                "exhaustive"
            ],
            "allOf": [
                {
                    "if": {
                        "properties": {
                            "exhaustive": {
                                "const": false
                            }
                        },
                        "required": [
                            "exhaustive"
                        ]
                    },
                    "then": {
                        "required": [
                            "limitation"
                        ]
                    }
                }
            ]
        },
        "InspectionLimitation": {
            "type": "object",
            "description": "Machine-readable evidence explaining why a source inspection is not exhaustive.",
            "properties": {
                "code": {
                    "type": "string",
                    "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)+$",
                    "description": "Stable namespaced reason code."
                },
                "message": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Human-readable explanation of what may be missing and why."
                },
                "details": {
                    "type": "object",
                    "description": "Optional implementation- or family-specific structured evidence.",
                    "additionalProperties": true
                }
            },
            "required": [
                "code",
                "message"
            ]
        }
    },
    "operations": {
        "openbindings.source-inspector.listBindingSpecs": {
            "description": "List the binding specifications this inspector can inspect, by exact identifier.",
            "idempotent": true,
            "output": {
                "type": "array",
                "items": {
                    "$ref": "#/schemas/BindingSpecInfo"
                }
            }
        },
        "openbindings.source-inspector.inspectSource": {
            "description": "Inspect a binding source artifact and return targets admitted by its governing binding specification, optionally with sound operation framing. The result states whether enumeration is exhaustive and explains any partial result.",
            "idempotent": true,
            "input": {
                "$ref": "#/schemas/InspectSourceInput"
            },
            "output": {
                "$ref": "#/schemas/SourceInspection"
            }
        }
    }
}
