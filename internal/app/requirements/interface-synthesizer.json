{
    "openbindings": "0.2.0",
    "name": "Interface Synthesizer",
    "version": "0.2.0",
    "description": "Interface for synthesizing OpenBindings interfaces from binding artifacts. Given a source artifact governed by a binding specification (e.g., an OpenAPI document), an interface synthesizer extracts operations, schemas, sources, and bindings into an OBI document.",
    "schemas": {
        "BindingSpecInfo": {
            "type": "object",
            "description": "Information about a supported binding specification.",
            "properties": {
                "bindingSpec": {
                    "type": "string",
                    "description": "Binding specification identifier, exact and opaque (e.g., openbindings.openapi@1, openbindings.mcp@1)."
                },
                "description": {
                    "type": "string",
                    "description": "Human-readable description of this binding specification."
                }
            },
            "required": [
                "bindingSpec"
            ]
        },
        "BindingSpecVerdict": {
            "type": "object",
            "description": "Authoritative support verdict for one exact binding-specification identifier. Future optional fields may annotate this verdict but never qualify or soften supported.",
            "properties": {
                "bindingSpec": {
                    "type": "string",
                    "description": "Exact binding-specification identifier token as supplied by the caller."
                },
                "supported": {
                    "type": "boolean",
                    "description": "Strict support verdict. True warrants that this implementation implements the specification denoted by the exact bindingSpec string, as published; false is an unqualified refusal."
                }
            },
            "required": [
                "bindingSpec",
                "supported"
            ]
        },
        "SynthesizeInterfaceSource": {
            "type": "object",
            "description": "A binding source to synthesize an interface from.",
            "properties": {
                "bindingSpec": {
                    "type": "string",
                    "description": "Binding specification identifier governing this source."
                },
                "name": {
                    "type": "string",
                    "description": "Key name for this binding source in the output."
                },
                "location": {
                    "type": "string",
                    "description": "Location to fetch content from (path or URL)."
                },
                "content": {
                    "description": "Content provided directly."
                },
                "outputLocation": {
                    "type": "string",
                    "description": "Location to reference in the output."
                },
                "embed": {
                    "type": "boolean",
                    "description": "If true, embed content inline in the output."
                },
                "description": {
                    "type": "string",
                    "description": "Human-readable description for this source."
                }
            },
            "required": [
                "bindingSpec"
            ],
            "anyOf": [
                {
                    "required": [
                        "location"
                    ]
                },
                {
                    "required": [
                        "content"
                    ]
                }
            ],
            "additionalProperties": false
        },
        "SynthesizeInterfaceInput": {
            "type": "object",
            "properties": {
                "openbindingsVersion": {
                    "type": "string",
                    "description": "Target OpenBindings spec version."
                },
                "sources": {
                    "type": "array",
                    "items": {
                        "$ref": "#/schemas/SynthesizeInterfaceSource"
                    },
                    "description": "Binding sources to synthesize from."
                },
                "name": {
                    "type": "string",
                    "description": "Interface name override."
                },
                "version": {
                    "type": "string",
                    "description": "Interface contract version override."
                },
                "description": {
                    "type": "string",
                    "description": "Interface description override."
                }
            },
            "additionalProperties": false
        },
        "OpenBindingsInterface": {
            "type": "object",
            "description": "A valid OpenBindings interface document.",
            "additionalProperties": true
        },
        "SynthesisCoverageEntry": {
            "type": "object",
            "description": "Disposition of one source interaction unit or separately tracked projection observed during synthesis. An interaction unit is an addressable target or independently selectable alternative whose omission would remove an upstream-permitted invocation path; a projection records schema or semantic fidelity of a represented path.",
            "properties": {
                "sourceIndex": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "Zero-based index of the input source containing the interaction unit."
                },
                "sourceKey": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Key of the corresponding source in the emitted interface. Required when status is represented or lossy."
                },
                "sourceRef": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Stable source-local identifier for the interaction unit. It need not be a conformant binding selector when the unit is excluded or invalid."
                },
                "scope": {
                    "type": "string",
                    "enum": [
                        "target",
                        "alternative",
                        "projection"
                    ],
                    "description": "Whether the unit is an addressable target, an independently selectable alternative of a target, or a schema/semantic projection whose fidelity is assessed separately."
                },
                "status": {
                    "type": "string",
                    "enum": [
                        "represented",
                        "excluded",
                        "invalid",
                        "lossy",
                        "implementation-unsupported"
                    ]
                },
                "operationKey": {
                    "type": "string",
                    "description": "Emitted operation key. Required when status is represented or lossy."
                },
                "bindingKey": {
                    "type": "string",
                    "description": "Emitted binding key. Required when status is represented or lossy."
                },
                "bindingSelector": {
                    "type": "string",
                    "description": "Emitted binding selector. Required when status is represented or lossy; the empty string denotes a binding-specification-defined omitted selector."
                },
                "reasonCode": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Stable family-namespaced reason code. Required unless status is represented."
                },
                "rule": {
                    "type": "string",
                    "description": "Binding-specification rule or section governing the disposition, when applicable."
                },
                "message": {
                    "type": "string",
                    "description": "Human-readable explanation."
                },
                "requirements": {
                    "type": "array",
                    "description": "Named runtime prerequisites for a represented unit, such as credentials or a required codec.",
                    "items": {
                        "type": "string",
                        "minLength": 1
                    }
                },
                "details": {
                    "type": "object",
                    "description": "Family-specific structured evidence.",
                    "additionalProperties": true
                }
            },
            "required": [
                "sourceIndex",
                "sourceRef",
                "scope",
                "status"
            ],
            "oneOf": [
                {
                    "properties": {
                        "status": {
                            "const": "represented"
                        }
                    },
                    "required": [
                        "sourceKey",
                        "operationKey",
                        "bindingKey",
                        "bindingSelector"
                    ]
                },
                {
                    "properties": {
                        "status": {
                            "const": "lossy"
                        }
                    },
                    "required": [
                        "sourceKey",
                        "operationKey",
                        "bindingKey",
                        "bindingSelector",
                        "reasonCode",
                        "message"
                    ]
                },
                {
                    "properties": {
                        "status": {
                            "enum": [
                                "excluded",
                                "invalid",
                                "implementation-unsupported"
                            ]
                        }
                    },
                    "required": [
                        "reasonCode",
                        "message"
                    ]
                }
            ]
        },
        "SynthesisCoverage": {
            "type": "object",
            "description": "Durable accounting of the source interaction units observed by one synthesis call.",
            "properties": {
                "entries": {
                    "type": "array",
                    "items": {
                        "$ref": "#/schemas/SynthesisCoverageEntry"
                    }
                },
                "exhaustive": {
                    "type": "boolean",
                    "description": "True only when every interaction unit in every accepted input source, under the governing binding revision's documented inventory, has exactly one disposition. False when the implementation cannot establish that claim."
                },
                "fullyRepresented": {
                    "type": "boolean",
                    "description": "True when exhaustive is true and every upstream-valid interaction unit is represented without a lossy or unsupported disposition. MUST agree with entries."
                },
                "limitation": {
                    "$ref": "#/schemas/CoverageLimitation"
                }
            },
            "required": [
                "entries",
                "exhaustive",
                "fullyRepresented"
            ],
            "oneOf": [
                {
                    "properties": {
                        "exhaustive": {
                            "const": true
                        }
                    }
                },
                {
                    "properties": {
                        "exhaustive": {
                            "const": false
                        }
                    },
                    "required": [
                        "limitation"
                    ]
                }
            ]
        },
        "CoverageLimitation": {
            "type": "object",
            "description": "Machine-readable evidence explaining why the synthesis inventory is not exhaustive.",
            "properties": {
                "code": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Stable namespaced reason code."
                },
                "message": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Human-readable explanation of what may be missing and why."
                },
                "details": {
                    "type": "object",
                    "description": "Optional implementation- or family-specific structured evidence.",
                    "additionalProperties": true
                }
            },
            "required": [
                "code",
                "message"
            ]
        },
        "SynthesizeInterfaceResult": {
            "type": "object",
            "description": "A creation-time-sound OBI and durable coverage evidence derived from the same synthesis observation.",
            "properties": {
                "interface": {
                    "$ref": "#/schemas/OpenBindingsInterface"
                },
                "coverage": {
                    "$ref": "#/schemas/SynthesisCoverage"
                }
            },
            "required": [
                "interface",
                "coverage"
            ]
        }
    },
    "operations": {
        "openbindings.interface-synthesizer.listBindingSpecs": {
            "description": "Return an advisory glance at binding specifications this synthesizer can name, for display, documentation, and pickers. Presence is a support warrant, and every listed identifier MUST receive supported: true from checkBindingSpecs. Absence carries no information and MUST NOT be treated as a negative support decision.",
            "idempotent": true,
            "output": {
                "type": "array",
                "items": {
                    "$ref": "#/schemas/BindingSpecInfo"
                }
            }
        },
        "openbindings.interface-synthesizer.checkBindingSpecs": {
            "description": "Authoritatively determine support for exact, opaque binding-specification identifier tokens. This is the only operation a consumer may use to make a support decision such as source acceptance, refusal, or delegation. The result contains one verdict per unique input token in first-occurrence order. supported is always a strict boolean; true warrants that this implementation implements the specification the exact string denotes, as published. The result is deterministic for a given implementation version and the operation is side-effect free. An internal pattern may compress the warranted set but MUST NOT extend it: an implementation never answers true for a specification it has not implemented and never matches future revisions by prefix or regular expression. Future optional verdict fields may annotate a verdict but never qualify or soften supported.",
            "idempotent": true,
            "input": {
                "type": "object",
                "properties": {
                    "bindingSpecs": {
                        "type": "array",
                        "items": {
                            "type": "string"
                        },
                        "description": "Exact, opaque binding-specification identifier tokens to check. The array may be empty."
                    }
                },
                "required": [
                    "bindingSpecs"
                ],
                "additionalProperties": false
            },
            "output": {
                "type": "array",
                "items": {
                    "$ref": "#/schemas/BindingSpecVerdict"
                }
            }
        },
        "openbindings.interface-synthesizer.synthesizeInterface": {
            "description": "Synthesize a creation-time-sound OpenBindings interface, optionally from binding source artifacts. Every emitted binding resolves to a supported target and admits a faithful invocation path. For every accepted source input, represent every callable target admitted by its governing binding revision; an implementation may refuse an unsupported artifact as a whole but MUST refuse instead of silently returning a partial interface when one admitted target cannot be represented faithfully.",
            "idempotent": true,
            "input": {
                "$ref": "#/schemas/SynthesizeInterfaceInput"
            },
            "output": {
                "$ref": "#/schemas/OpenBindingsInterface"
            }
        },
        "openbindings.interface-synthesizer.synthesizeInterfaceWithCoverage": {
            "description": "Synthesize a creation-time-sound OpenBindings interface and return durable dispositions plus an explicit exhaustiveness claim for the source interaction units considered by the same call. The result distinguishes binding-specification exclusions, invalid source units, lossy projections, and implementation gaps. Coverage is evidence that consumers may verify independently, not proof.",
            "idempotent": true,
            "input": {
                "$ref": "#/schemas/SynthesizeInterfaceInput"
            },
            "output": {
                "$ref": "#/schemas/SynthesizeInterfaceResult"
            }
        }
    }
}
