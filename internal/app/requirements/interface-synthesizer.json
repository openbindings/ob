{
    "openbindings": "0.2.0",
    "name": "Interface Synthesizer",
    "version": "0.2.0",
    "description": "Interface for synthesizing OpenBindings interfaces from binding artifacts. Given a binding specification (e.g., an OpenAPI spec), an interface synthesizer extracts operations, schemas, sources, and bindings into an OBI document.",
    "schemas": {
        "BindingSpecInfo": {
            "type": "object",
            "description": "Information about a supported binding specification.",
            "properties": {
                "bindingSpec": {
                    "type": "string",
                    "description": "Binding specification identifier, exact and opaque (e.g., openbindings.openapi@1, openbindings.mcp@1)."
                },
                "description": {
                    "type": "string",
                    "description": "Human-readable description of this binding specification."
                }
            },
            "required": [
                "bindingSpec"
            ]
        },
        "SynthesizeInterfaceSource": {
            "type": "object",
            "description": "A binding source to synthesize an interface from.",
            "properties": {
                "bindingSpec": {
                    "type": "string",
                    "description": "Binding specification identifier governing this source."
                },
                "name": {
                    "type": "string",
                    "description": "Key name for this binding source in the output."
                },
                "location": {
                    "type": "string",
                    "description": "Location to fetch content from (path or URL)."
                },
                "content": {
                    "description": "Content provided directly."
                },
                "outputLocation": {
                    "type": "string",
                    "description": "Location to reference in the output."
                },
                "embed": {
                    "type": "boolean",
                    "description": "If true, embed content inline in the output."
                },
                "description": {
                    "type": "string",
                    "description": "Human-readable description for this source."
                }
            },
            "required": [
                "bindingSpec"
            ],
            "anyOf": [
                {
                    "required": [
                        "location"
                    ]
                },
                {
                    "required": [
                        "content"
                    ]
                }
            ],
            "additionalProperties": false
        },
        "SynthesizeInterfaceInput": {
            "type": "object",
            "properties": {
                "openbindingsVersion": {
                    "type": "string",
                    "description": "Target OpenBindings spec version."
                },
                "sources": {
                    "type": "array",
                    "items": {
                        "$ref": "#/schemas/SynthesizeInterfaceSource"
                    },
                    "description": "Binding sources to synthesize from."
                },
                "name": {
                    "type": "string",
                    "description": "Interface name override."
                },
                "version": {
                    "type": "string",
                    "description": "Interface contract version override."
                },
                "description": {
                    "type": "string",
                    "description": "Interface description override."
                }
            },
            "additionalProperties": false
        },
        "OpenBindingsInterface": {
            "type": "object",
            "description": "A valid OpenBindings interface document.",
            "additionalProperties": true
        }
    },
    "operations": {
        "openbindings.interface-synthesizer.listBindingSpecs": {
            "description": "List the binding specifications this synthesizer can synthesize interfaces from, by exact identifier.",
            "idempotent": true,
            "output": {
                "type": "array",
                "items": {
                    "$ref": "#/schemas/BindingSpecInfo"
                }
            }
        },
        "openbindings.interface-synthesizer.synthesizeInterface": {
            "description": "Synthesize an OpenBindings interface, optionally from binding source artifacts.",
            "idempotent": true,
            "input": {
                "$ref": "#/schemas/SynthesizeInterfaceInput"
            },
            "output": {
                "$ref": "#/schemas/OpenBindingsInterface"
            }
        }
    }
}
