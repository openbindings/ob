{
  "openbindings": "0.2.0",
  "name": "ob",
  "version": "0.1.0",
  "description": "The complete interface implemented by ob, the OpenBindings reference tool. One unbound contract describing everything ob does: authoring, inspecting, editing, validating, comparing, and invoking OpenBindings interfaces, plus context storage and environment management. This document carries no bindings or sources; it is the single source of truth from which the per-surface realizations (CLI via usage, server via OpenAPI/AsyncAPI/MCP) are derived, so they cannot drift. Every operation is keyed under ob's own namespace (e.g. openbindings.ob.pullSource). An operation that satisfies a published OpenBindings interface carries that contract's operation key as an alias (openbindings.ob.setContext carries openbindings.key-value-store.set), and one operation may satisfy several contracts this way, as listFormats does.",
  "operations": {
    "openbindings.ob.describe": {
      "description": "Return identity and metadata about ob: name, version, description, homepage, repository, maintainer.",
      "idempotent": true,
      "output": {
        "$ref": "#/schemas/SoftwareIdentity"
      },
      "aliases": [
        "openbindings.software-descriptor.describe"
      ]
    },
    "openbindings.ob.listFormats": {
      "description": "List the binding formats this instance can handle. One operation satisfies the listFormats slot of every interface that has one, since the answer is the same regardless of which capability is asking.",
      "aliases": [
        "openbindings.binding-invoker.listFormats",
        "openbindings.interface-synthesizer.listFormats",
        "openbindings.source-inspector.listFormats"
      ],
      "idempotent": true,
      "output": {
        "type": "array",
        "items": {
          "$ref": "#/schemas/FormatInfo"
        }
      }
    },
    "openbindings.ob.resolveInterface": {
      "description": "Resolve an OpenBindings interface from a URL or host. Performs well-known discovery, follows redirects, and synthesizes an OBI from a raw binding spec (e.g. an OpenAPI document) when the target is not already an OBI. Returns the interface and metadata about how it was resolved.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ResolveInterfaceInput"
      },
      "output": {
        "$ref": "#/schemas/ResolveInterfaceOutput"
      }
    },
    "openbindings.ob.synthesizeInterface": {
      "description": "Synthesize an OpenBindings interface, optionally from binding source artifacts. Given one or more sources (e.g. an OpenAPI spec, a CLI usage spec), extracts operations, schemas, sources, and bindings into the new OBI; with no sources, produces an empty interface to author by hand.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/SynthesizeInterfaceInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      },
      "aliases": [
        "openbindings.interface-synthesizer.synthesizeInterface"
      ]
    },
    "openbindings.ob.inspectSource": {
      "description": "Inspect a binding source artifact and report the bindable targets it contains, before any interface is created. Lets tooling show a user what an artifact offers.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/InspectSourceInput"
      },
      "output": {
        "$ref": "#/schemas/SourceInspection"
      },
      "aliases": [
        "openbindings.source-inspector.inspectSource"
      ]
    },
    "openbindings.ob.addSource": {
      "description": "Register a binding source reference on an interface and return the modified interface.",
      "input": {
        "$ref": "#/schemas/AddSourceInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.removeSource": {
      "description": "Remove a source reference and the bindings that reference it from an interface; operations are always preserved. Returns the modified interface.",
      "input": {
        "$ref": "#/schemas/SourceKeyInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.listSources": {
      "description": "List the source references on an interface.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/InterfaceInput"
      },
      "output": {
        "type": "array",
        "items": {
          "$ref": "#/schemas/SourceListEntry"
        }
      }
    },
    "openbindings.ob.addOperation": {
      "description": "Add an operation with the given key to an interface and return the modified interface.",
      "input": {
        "$ref": "#/schemas/AddOperationInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.renameOperation": {
      "description": "Rename an operation and update every reference to it across the interface — binding operation fields and binding keys following the <operation>.<source> convention. Returns the modified interface.",
      "input": {
        "$ref": "#/schemas/RenameOperationInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.removeOperation": {
      "description": "Remove one or more operations and their bindings from an interface and return the modified interface.",
      "input": {
        "$ref": "#/schemas/RemoveOperationInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.listOperations": {
      "description": "List the operations on an interface, optionally filtered by tag.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ListOperationsInput"
      },
      "output": {
        "type": "array",
        "items": {
          "$ref": "#/schemas/OperationListEntry"
        }
      }
    },
    "openbindings.ob.mergeInterfaces": {
      "description": "Selectively apply changes from one interface (or from the target's own sources) into another, and return the merged interface alongside a report of what was applied and skipped.",
      "input": {
        "$ref": "#/schemas/MergeInterfacesInput"
      },
      "output": {
        "$ref": "#/schemas/MergeResult"
      }
    },
    "openbindings.ob.validateInterface": {
      "description": "Validate an OpenBindings interface document against the specification. Reports whether it is valid, the declared spec version, and any problems. A document declaring a spec version newer than the tool supports is refused in every mode (OBI-T-04); strict mode additionally rejects unknown (non-x-) fields.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ValidateInterfaceInput"
      },
      "output": {
        "$ref": "#/schemas/ValidationReport"
      }
    },
    "openbindings.ob.compareInterfaces": {
      "description": "Structurally compare two interfaces (or one interface against what its sources produce) and report the differences per operation and metadata field, including cross-source drift.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/CompareInterfacesInput"
      },
      "output": {
        "$ref": "#/schemas/DiffReport"
      }
    },
    "openbindings.ob.reportCompatibility": {
      "description": "Report whether a candidate interface conforms to a target interface, as an ob-comparison-report/v1 document: paired operations with directional schema verdicts and findings, a summary verdict, coverage, and category counts. A report, not a binary verdict. The target rides the report's left side, the candidate its right.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ReportCompatibilityInput"
      },
      "output": {
        "$ref": "#/schemas/CompatibilityReport"
      }
    },
    "openbindings.ob.invokeBinding": {
      "description": "Invoke a resolved binding via a typed bidirectional I/O frame stream (the binding-invoker frame protocol). The caller streams BindingInvokerInputFrame messages (one `open` carrying the BindingInvocationInput, zero or more `input`, one `close`); the service streams BindingInvokerOutputFrame messages back (zero or more `output`/`input_closed`, then exactly one terminal `complete` or `error`). One shape covers unary, server-streaming, client-streaming, and bidirectional bindings; cardinality is observed by how the caller drives the frames. Missing runtime context fails with a terminal CONTEXT_REQUIRED error carrying ContextRequiredDetails, before any output and any side effect.",
      "input": {
        "$ref": "#/schemas/BindingInvokerInputFrame"
      },
      "output": {
        "$ref": "#/schemas/BindingInvokerOutputFrame"
      },
      "aliases": [
        "openbindings.binding-invoker.invokeBinding"
      ]
    },
    "openbindings.ob.prepareBinding": {
      "description": "Preflight a binding: report the context it would require before invocation, without invoking it or causing side effects. Returns ContextRequiredDetails the runtime can resolve proactively, or null when requirements cannot be determined statically. Advisory; invokeBinding's reactive CONTEXT_REQUIRED error is authoritative. When context is supplied, reports only the requirements still unsatisfied by it.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/BindingInvocationInput"
      },
      "output": {
        "oneOf": [
          {
            "$ref": "#/schemas/ContextRequiredDetails"
          },
          {
            "type": "null"
          }
        ]
      },
      "aliases": [
        "openbindings.binding-invoker.prepareBinding"
      ]
    },
    "openbindings.ob.invokeOperation": {
      "description": "Invoke an operation on an interface via a typed bidirectional I/O frame stream (the operation-invoker frame protocol). Unlike invokeBinding, the caller names an operation (or a specific binding) on a supplied interface, and the invoker resolves the name, selects a binding, validates against the operation's schemas, applies its transforms, and drives the underlying binding invoker. The frame shape mirrors invokeBinding exactly: one `open` carrying the OperationInvocationInput, then input frames and a close; output frames plus a terminal complete or error. Cardinality is observed, not declared, so a request/response and a streaming binding can satisfy the same operation identically. Missing context fails with a terminal CONTEXT_REQUIRED error before any side effect.",
      "input": {
        "$ref": "#/schemas/OperationInvokerInputFrame"
      },
      "output": {
        "$ref": "#/schemas/OperationInvokerOutputFrame"
      },
      "aliases": [
        "openbindings.operation-invoker.invokeOperation"
      ]
    },
    "openbindings.ob.prepareOperation": {
      "description": "Operation-level preflight: report the context invoking an operation would require, without invoking it or causing side effects. The by-reference counterpart to prepareBinding. Resolves the named operation (or binding) on the supplied interface to a concrete binding and reports that binding's ContextRequiredDetails, or null when requirements cannot be determined without invoking. Returning null is always conformant. Advisory: invokeOperation's reactive CONTEXT_REQUIRED is authoritative. When context is supplied, the result reports only the requirements still unsatisfied by it.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/OperationInvocationInput"
      },
      "output": {
        "oneOf": [
          {
            "$ref": "#/schemas/ContextRequiredDetails"
          },
          {
            "type": "null"
          }
        ]
      },
      "aliases": [
        "openbindings.operation-invoker.prepareOperation"
      ]
    },
    "openbindings.ob.getContext": {
      "description": "Get the stored context for a key. Returns the unified context payload (credentials, headers, cookies, environment, metadata in a single opaque map), or null when no context matches. Lookup on URL keys is hierarchical, like invocation-time matching: the exact key wins, then the store walks up the URL path to the most specific stored prefix. Exact keys behave as a plain key-value get, so the key-value-store alias holds.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ContextKeyInput"
      },
      "output": {
        "oneOf": [
          {
            "$ref": "#/schemas/Context"
          },
          {
            "type": "null"
          }
        ]
      },
      "aliases": [
        "openbindings.key-value-store.get"
      ]
    },
    "openbindings.ob.setContext": {
      "description": "Create or replace the context for a key. The supplied value fully replaces any existing context (full replacement, not partial update).",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/SetContextInput"
      },
      "aliases": [
        "openbindings.key-value-store.set"
      ]
    },
    "openbindings.ob.removeContext": {
      "description": "Remove the stored context for a key. Deleting an absent key succeeds.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ContextKeyInput"
      },
      "aliases": [
        "openbindings.key-value-store.delete"
      ]
    },
    "openbindings.ob.listContexts": {
      "description": "List non-secret metadata summaries for all stored contexts; never returns secret values. Completes the context surface with listing and inspection: the generic key-value-store substrate these contexts are stored under (satisfied by getContext/setContext/removeContext) standardizes only opaque get/set/delete, so this management view is ob's own.",
      "idempotent": true,
      "output": {
        "type": "array",
        "items": {
          "$ref": "#/schemas/ContextSummary"
        }
      }
    },
    "openbindings.ob.registerDelegate": {
      "description": "Register a delegate \u2014 any referenceable OpenBindings interface (an exec: or http(s) location) that ob may route operations to. ob resolves the location to the delegate's OBI and records a snapshot: the operation identifiers it carries (keys and aliases), a content digest pinning the resolved document, and what ob derives for its own routing \u2014 which of ob's format capabilities the delegate satisfies (checked by conformance, not name-matching) and the formats it handles. Registration fails when the location cannot be resolved to an interface. Re-registering a location re-resolves and replaces the snapshot; the registrar's preferences persist unless a new preference is supplied. An OBI carrying none of ob's three format capabilities registers successfully but is inert for ob; registration warns.",
      "aliases": [
        "openbindings.delegate-manager.registerDelegate"
      ],
      "input": {
        "$ref": "#/schemas/RegisterDelegateInput"
      },
      "output": {
        "$ref": "#/schemas/DelegateSummary"
      }
    },
    "openbindings.ob.unregisterDelegate": {
      "description": "Remove a registered delegate by its location. Idempotent: unregistering a location that is not registered succeeds.",
      "aliases": [
        "openbindings.delegate-manager.unregisterDelegate"
      ],
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/DelegateLocationInput"
      },
      "output": {
        "type": "null"
      }
    },
    "openbindings.ob.listDelegates": {
      "description": "List every registered delegate \u2014 the self-delegate included \u2014 with the operations each carried when last resolved, the capabilities and formats ob derived for routing, and its selection preferences.",
      "aliases": [
        "openbindings.delegate-manager.listDelegates"
      ],
      "idempotent": true,
      "output": {
        "type": "object",
        "properties": {
          "delegates": {
            "type": "array",
            "items": {
              "$ref": "#/schemas/DelegateSummary"
            },
            "description": "The registered delegates."
          }
        },
        "required": [
          "delegates"
        ],
        "additionalProperties": false
      }
    },
    "openbindings.ob.resolveDelegate": {
      "description": "Resolve an operation to the registered delegates that carry it \u2014 those whose interface answers to the operation's key or an alias \u2014 ordered by effective preference: the per-operation override when set, else the delegate-level preference, else the baseline; ties favor the self-delegate, then registration order. Returns candidates only; it does not invoke anything, and ob's routing applies its own narrowing (format support) on top.",
      "aliases": [
        "openbindings.delegate-manager.resolveDelegate"
      ],
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ResolveDelegateInput"
      },
      "output": {
        "$ref": "#/schemas/ResolveDelegateResult"
      }
    },
    "openbindings.ob.resolveDelegateForFormat": {
      "description": "Report which delegate ob's routing would select for a format token, and the capabilities it offers for it \u2014 ob's format-narrowing diagnostic, layered on top of the operation-keyed resolveDelegate.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ResolveDelegateForFormatInput"
      },
      "output": {
        "$ref": "#/schemas/ResolveDelegateForFormatResult"
      }
    },
    "openbindings.ob.getDelegateRequirements": {
      "description": "Return the published interface a delegate must satisfy to provide a capability (invoke \u2192 binding-invoker, synthesize \u2192 interface-synthesizer, inspect \u2192 source-inspector), so a prospective delegate can be checked against the exact contract (e.g. via reportCompatibility). The interfaces are immutable documents bundled with this ob release.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/GetDelegateRequirementsInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.setDelegatePreference": {
      "description": "Set or clear a delegate's selection preference (higher = more preferred; 0 is the baseline; negatives rank below it; null clears). Without an operation, sets the delegate-level default; with one, sets that operation's entry in the delegate's preference index. ob additionally accepts a binding-source format scope on an operation entry \u2014 how synthesis of one format can prefer one delegate while everything else prefers another. Preference governs the order resolveDelegate returns candidates in; routing applies OBI-T-09's selection semantics to delegates \u2014 ob's own narrowing, not a spec rule \u2014 with effective preference the per-operation value, else the delegate-level value, else 0. Returns the updated summary.",
      "aliases": [
        "openbindings.delegate-manager.setDelegatePreference"
      ],
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/SetDelegatePreferenceInput"
      },
      "output": {
        "$ref": "#/schemas/DelegateSummary"
      }
    },
    "openbindings.ob.initializeEnvironment": {
      "description": "Initialize an OpenBindings environment (a local .openbindings/ directory, or the global one with global) and return the created environment's status.",
      "input": {
        "$ref": "#/schemas/InitializeEnvironmentInput"
      },
      "output": {
        "$ref": "#/schemas/EnvironmentStatus"
      }
    },
    "openbindings.ob.reportEnvironmentStatus": {
      "description": "Report a snapshot of the active OpenBindings environment (the .openbindings/ directory): whether it is local or global, its path, the number of delegates it holds, and the number of contexts in the user's context store (contexts are user-scoped, not per-environment).",
      "idempotent": true,
      "output": {
        "$ref": "#/schemas/EnvironmentStatus"
      }
    },
    "openbindings.ob.reportInterfaceStatus": {
      "description": "Report an interface's status against its sources: per-source sync state (in-sync, tracked, what a pull would change) and the source-owned vs hand-authored breakdown of its operations and bindings. Read-only \u2014 distinct from pullSource (which re-derives and writes) and from compareInterfaces with fromSources (which reports raw differences).",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/InterfaceStatusInput"
      },
      "output": {
        "$ref": "#/schemas/InterfaceStatus"
      }
    },
    "openbindings.ob.startServer": {
      "description": "Start a local server that exposes ob's operations over HTTP and HTTPS (and the binding-invoker frame protocol). A foreground, environment-local operation; not meaningfully invoked over a remote transport.",
      "input": {
        "$ref": "#/schemas/StartServerInput"
      },
      "output": {
        "$ref": "#/schemas/ServerInfo"
      }
    },
    "openbindings.ob.startMCPServer": {
      "description": "Serve an interface URL as an MCP server, exposing its operations as MCP tools (named by the sanitized full operation key) over stdio or HTTP. To bridge several services, compose them into one aggregate interface first. A foreground, environment-local operation, the MCP sibling of startServer; not meaningfully invoked over a remote transport.",
      "input": {
        "$ref": "#/schemas/StartMCPServerInput"
      },
      "output": {
        "oneOf": [
          {
            "$ref": "#/schemas/ServerInfo"
          },
          {
            "type": "null"
          }
        ]
      }
    },
    "openbindings.ob.codegen": {
      "description": "Generate a typed invoker for an interface in a target language (TypeScript or Go). The output is an invoker over the cardinality-agnostic handle, not a cardinality-specific client.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/CodegenInput"
      },
      "output": {
        "$ref": "#/schemas/CodegenOutput"
      }
    },
    "openbindings.ob.conform": {
      "description": "Scaffold a target interface so it conforms to a reference interface: add the operations the reference requires and reconcile mismatches, returning the updated target and a report of each change. With dryRun, reports the changes without applying them.",
      "input": {
        "$ref": "#/schemas/ConformInput"
      },
      "output": {
        "$ref": "#/schemas/ConformResult"
      }
    },
    "openbindings.ob.demo": {
      "description": "Start the bundled demonstration servers (HTTP and gRPC) that showcase one interface satisfied across multiple binding formats. A foreground, environment-local operation, like startServer; not meaningfully invoked over a remote transport.",
      "input": {
        "$ref": "#/schemas/DemoInput"
      }
    },
    "openbindings.ob.addOperationAlias": {
      "description": "Declare satisfaction alias(es) on an operation (OBI-T-12) and return the modified interface.",
      "input": {
        "$ref": "#/schemas/AddOperationAliasInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.removeOperationAlias": {
      "description": "Withdraw satisfaction alias(es) from an operation and return the modified interface.",
      "input": {
        "$ref": "#/schemas/RemoveOperationAliasInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.listOperationAliases": {
      "description": "List the satisfaction map \u2014 each operation and the interface operations it satisfies via aliases.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/ListOperationAliasesInput"
      },
      "output": {
        "type": "array",
        "items": {
          "$ref": "#/schemas/OperationAliasEntry"
        }
      }
    },
    "openbindings.ob.pullSource": {
      "description": "Derive operations and bindings from registered sources into the interface \u2014 create on first pull, overwrite + prune source-owned objects on later pulls, leave hand-authored objects untouched. Returns the resulting interface document plus the change log of affected operation and binding keys.",
      "input": {
        "$ref": "#/schemas/PullSourceInput"
      },
      "output": {
        "$ref": "#/schemas/SourcePullOutput"
      }
    },
    "openbindings.ob.purifyInterface": {
      "description": "Strip x-ob vendor metadata from an interface, yielding a spec-only document suitable for publishing.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/OpenBindingsInterface"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.setOperation": {
      "description": "Edit an existing operation's fields (description, idempotency, deprecation, schemas, tags) and return the modified interface. A source-owned operation requires own=true (detaches first).",
      "input": {
        "$ref": "#/schemas/SetOperationInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.detachOperation": {
      "description": "Convert a source-owned operation into a hand-authored one (so pull no longer overwrites it) and return the modified interface.",
      "input": {
        "$ref": "#/schemas/DetachOperationInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.setOperationCodegenName": {
      "description": "Set (or clear) the symbol name 'ob codegen' emits for an operation, stored in the operation's x-ob.codegenName metadata, and return the modified interface. The operation key is unchanged; the override survives source pull and is stripped by purify.",
      "input": {
        "$ref": "#/schemas/SetOperationCodegenNameInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.setOperationOutputSchema": {
      "description": "Elect an operation's real output schema (the non-detaching remedy for a floor-stamped synthesis), writing it into the operation's output and stamping an x-ob election marker, and return the modified interface. The marker survives source pull (re-applied onto each fresh derivation; a grown, non-floor-stamped source schema wins and displaces it) and is stripped by purify.",
      "input": {
        "$ref": "#/schemas/SetOperationOutputSchemaInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.bindOperation": {
      "description": "Attach a source ref to an existing operation (preserving the operation's key) and return the modified interface. Warns and optionally scaffolds transform stubs on a shape mismatch.",
      "input": {
        "$ref": "#/schemas/BindOperationInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.unbindOperation": {
      "description": "Remove a binding from an operation without removing the operation, and return the modified interface.",
      "input": {
        "$ref": "#/schemas/UnbindOperationInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.newInterface": {
      "description": "Create an empty interface document (core fields + empty operations map) and return it.",
      "idempotent": true,
      "input": {
        "$ref": "#/schemas/NewInterfaceInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    },
    "openbindings.ob.setMetadata": {
      "description": "Edit an interface's top-level metadata (name, version, description) and return the modified interface.",
      "input": {
        "$ref": "#/schemas/SetMetadataInput"
      },
      "output": {
        "$ref": "#/schemas/OpenBindingsInterface"
      }
    }
  },
  "schemas": {
    "OpenBindingsInterface": {
      "type": "object",
      "description": "A valid OpenBindings interface document.",
      "additionalProperties": true
    },
    "InterfaceInput": {
      "type": "object",
      "description": "Wraps the interface document an operation acts on.",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "SoftwareIdentity": {
      "type": "object",
      "description": "Identity and metadata for a piece of software.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable display name."
        },
        "version": {
          "type": "string",
          "description": "Software version."
        },
        "description": {
          "type": "string",
          "description": "Brief description."
        },
        "homepage": {
          "type": "string",
          "description": "Documentation URL."
        },
        "repository": {
          "type": "string",
          "description": "Source code URL."
        },
        "maintainer": {
          "type": "string",
          "description": "Author or organization."
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": true
    },
    "FormatInfo": {
      "type": "object",
      "description": "Information about a supported binding format.",
      "properties": {
        "token": {
          "type": "string",
          "description": "Format token (e.g. openapi@3.1, asyncapi@3.0, grpc)."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of this format."
        }
      },
      "required": [
        "token"
      ],
      "additionalProperties": false
    },
    "ResolveInterfaceInput": {
      "type": "object",
      "properties": {
        "address": {
          "type": "string",
          "description": "Base URL or host to resolve from (e.g. https://api.example.com or localhost:8080)."
        }
      },
      "required": [
        "address"
      ],
      "additionalProperties": false
    },
    "ResolveInterfaceOutput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "synthesizedFrom": {
          "type": "string",
          "description": "The binding-format token the interface was synthesized from (e.g. openapi@3.1) when the target was not already an OBI; absent for native OBIs."
        },
        "resolvedUrl": {
          "type": "string",
          "description": "The URL the interface document (or the source spec it was synthesized from) was ultimately fetched from."
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "Source": {
      "type": "object",
      "description": "A binding source artifact, referenced by location or carried inline.",
      "properties": {
        "format": {
          "type": "string",
          "description": "Format token (e.g. openapi@3.1)."
        },
        "location": {
          "type": "string",
          "description": "Location of the source (path or URL)."
        },
        "content": {
          "description": "Source content provided directly."
        },
        "description": {
          "type": "string"
        }
      },
      "required": [
        "format"
      ],
      "anyOf": [
        {
          "required": [
            "location"
          ]
        },
        {
          "required": [
            "content"
          ]
        }
      ],
      "additionalProperties": true
    },
    "SynthesizeInterfaceSource": {
      "type": "object",
      "description": "A binding source to extract operations from.",
      "properties": {
        "format": {
          "type": "string",
          "description": "Format token (e.g. openapi@3.1, usage@2.13.1)."
        },
        "name": {
          "type": "string",
          "description": "Key for this source in the output interface's sources."
        },
        "location": {
          "type": "string",
          "description": "Location to fetch content from (path or URL)."
        },
        "content": {
          "description": "Content provided directly."
        },
        "outputLocation": {
          "type": "string",
          "description": "Location to reference in the output interface's source."
        },
        "embed": {
          "type": "boolean",
          "description": "If true, embed content inline in the output interface."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description for this source."
        }
      },
      "required": [
        "format"
      ],
      "anyOf": [
        {
          "required": [
            "location"
          ]
        },
        {
          "required": [
            "content"
          ]
        }
      ],
      "additionalProperties": false
    },
    "SynthesizeInterfaceInput": {
      "type": "object",
      "properties": {
        "openbindingsVersion": {
          "type": "string",
          "description": "Target OpenBindings spec version (e.g. 0.2.0)."
        },
        "sources": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/SynthesizeInterfaceSource"
          },
          "description": "Binding sources to extract from."
        },
        "name": {
          "type": "string",
          "description": "Interface name override."
        },
        "version": {
          "type": "string",
          "description": "Interface contract version override."
        },
        "description": {
          "type": "string",
          "description": "Interface description override."
        }
      },
      "additionalProperties": false
    },
    "InspectSourceInput": {
      "type": "object",
      "properties": {
        "source": {
          "$ref": "#/schemas/Source"
        }
      },
      "required": [
        "source"
      ],
      "additionalProperties": false
    },
    "SourceInspection": {
      "type": "object",
      "properties": {
        "targets": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/BindableTarget"
          }
        },
        "exhaustive": {
          "type": "boolean",
          "description": "True if every bindable target in the source is listed."
        }
      },
      "required": [
        "targets",
        "exhaustive"
      ],
      "additionalProperties": true
    },
    "BindableTarget": {
      "type": "object",
      "properties": {
        "ref": {
          "type": "string",
          "description": "Reference within the source identifying this target."
        },
        "operationKey": {
          "type": "string",
          "description": "Suggested operation key for this target."
        },
        "operation": {
          "$ref": "#/schemas/Operation"
        }
      },
      "required": [
        "ref"
      ],
      "additionalProperties": true
    },
    "Operation": {
      "type": "object",
      "description": "An operation definition as it would appear in an interface.",
      "properties": {
        "description": {
          "type": "string"
        },
        "input": {},
        "output": {},
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "deprecated": {
          "type": "boolean"
        }
      },
      "additionalProperties": true
    },
    "AddSourceInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "source": {
          "$ref": "#/schemas/SynthesizeInterfaceSource"
        }
      },
      "required": [
        "interface",
        "source"
      ],
      "additionalProperties": false
    },
    "SourceKeyInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "key": {
          "type": "string",
          "description": "Source key."
        }
      },
      "required": [
        "interface",
        "key"
      ],
      "additionalProperties": false
    },
    "AddOperationInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "key": {
          "type": "string",
          "description": "Key for the new operation."
        },
        "description": {
          "type": "string"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "input": {
          "description": "JSON Schema for the new operation's input."
        },
        "output": {
          "description": "JSON Schema for the new operation's output."
        },
        "idempotent": {
          "type": "boolean",
          "description": "Whether the new operation is idempotent."
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Satisfaction aliases \u2014 other interfaces' operation keys this operation also answers to (OBI-T-12)."
        }
      },
      "required": [
        "interface",
        "key"
      ],
      "additionalProperties": false
    },
    "RenameOperationInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "oldKey": {
          "type": "string"
        },
        "newKey": {
          "type": "string"
        }
      },
      "required": [
        "interface",
        "oldKey",
        "newKey"
      ],
      "additionalProperties": false
    },
    "RemoveOperationInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "keys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "Operation keys to remove."
        },
        "force": {
          "type": "boolean",
          "description": "Remove source-owned operations without warning."
        }
      },
      "required": [
        "interface",
        "keys"
      ],
      "additionalProperties": false
    },
    "ListOperationsInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "tag": {
          "type": "string",
          "description": "Filter operations by tag."
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "MergeInterfacesInput": {
      "type": "object",
      "properties": {
        "target": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "source": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "fromSources": {
          "type": "boolean",
          "description": "Derive the source side from the target's own binding sources."
        },
        "only": {
          "type": "string",
          "description": "Scope to a specific source key (requires fromSources)."
        },
        "operations": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Only merge these operations."
        },
        "excludeOperations": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Exclude these operations from the merge."
        },
        "opsOnly": {
          "type": "boolean",
          "description": "Graft operations only (no bindings or source entries) \u2014 the adopt-keys satisfaction strategy."
        },
        "noBindings": {
          "type": "boolean",
          "description": "Don't graft bindings (operations and their referenced sources still come)."
        },
        "noSources": {
          "type": "boolean",
          "description": "Don't graft source entries (operations and bindings still come)."
        }
      },
      "required": [
        "target"
      ],
      "additionalProperties": false
    },
    "MergeResult": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/MergeEntry"
          }
        },
        "applied": {
          "type": "integer",
          "description": "Number of changes applied."
        },
        "skipped": {
          "type": "integer",
          "description": "Number of changes skipped."
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "dryRun": {
          "type": "boolean",
          "description": "Whether this was a preview run (changes reported, not applied)."
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "ValidateInterfaceInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "strict": {
          "type": "boolean",
          "description": "Additionally reject unknown (non-x-) fields. A spec version newer than the tool supports is refused in every mode."
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "ValidationReport": {
      "type": "object",
      "properties": {
        "valid": {
          "type": "boolean"
        },
        "version": {
          "type": "string",
          "description": "The spec version the document declares."
        },
        "problems": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "valid"
      ],
      "additionalProperties": true
    },
    "CompareInterfacesInput": {
      "type": "object",
      "properties": {
        "baseline": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "comparison": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "fromSources": {
          "type": "boolean",
          "description": "Compare the baseline against what its binding sources produce."
        },
        "only": {
          "type": "string",
          "description": "Scope to a specific source key (requires fromSources)."
        }
      },
      "required": [
        "baseline"
      ],
      "additionalProperties": false
    },
    "DiffReport": {
      "type": "object",
      "description": "The differences between two interfaces (or an interface and what its sources produce). Empty operations, metadata, and drift means they are identical.",
      "properties": {
        "operations": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/OperationDiff"
          }
        },
        "metadata": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/MetadataDiff"
          }
        },
        "drift": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/DriftEntry"
          }
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "OperationDiff": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "added",
            "removed",
            "changed"
          ]
        },
        "details": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "What specifically changed."
        }
      },
      "required": [
        "operation",
        "status"
      ],
      "additionalProperties": false
    },
    "MetadataDiff": {
      "type": "object",
      "properties": {
        "field": {
          "type": "string"
        },
        "baseline": {
          "type": "string"
        },
        "compared": {
          "type": "string"
        }
      },
      "required": [
        "field"
      ],
      "additionalProperties": false
    },
    "DriftEntry": {
      "type": "object",
      "description": "One operation key produced by multiple sources with differing schemas.",
      "properties": {
        "operation": {
          "type": "string"
        },
        "sources": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/DriftSource"
          }
        },
        "details": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "operation",
        "sources"
      ],
      "additionalProperties": false
    },
    "DriftSource": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "format": {
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "ReportCompatibilityInput": {
      "type": "object",
      "properties": {
        "target": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "candidate": {
          "$ref": "#/schemas/OpenBindingsInterface"
        }
      },
      "required": [
        "target",
        "candidate"
      ],
      "additionalProperties": false
    },
    "CompatibilityReport": {
      "type": "object",
      "description": "An ob-comparison-report/v1 document. The target interface rides the report's left side, the candidate its right; operations are paired by key+alias resolution (OBI-T-12) and judged directionally. A report, not a binary verdict: the summary verdict is compatible, incompatible, or indeterminate. Reserved section arrays (schemas, metadata, sources, bindings, transforms, security) accompany operations.",
      "properties": {
        "format_version": {
          "type": "string",
          "description": "Report convention identifier (ob-comparison-report/v1)."
        },
        "profile": {
          "type": "string",
          "description": "Schema comparison profile and hash (e.g. OB-2020-12@local)."
        },
        "mode": {
          "type": "string",
          "description": "Comparison mode (e.g. subsume)."
        },
        "tool": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "version": {
              "type": "string"
            }
          },
          "required": [
            "name",
            "version"
          ],
          "additionalProperties": false
        },
        "generated_at": {
          "type": "string"
        },
        "inputs": {
          "type": "object",
          "description": "Provenance descriptors for the two compared documents.",
          "properties": {
            "left": {
              "$ref": "#/schemas/ComparisonInputDescriptor"
            },
            "right": {
              "$ref": "#/schemas/ComparisonInputDescriptor"
            }
          },
          "required": [
            "left",
            "right"
          ],
          "additionalProperties": false
        },
        "summary": {
          "type": "object",
          "properties": {
            "verdict": {
              "type": "string",
              "enum": [
                "compatible",
                "incompatible",
                "indeterminate"
              ]
            },
            "coverage": {
              "type": "object",
              "properties": {
                "total_operations": {
                  "type": "integer"
                },
                "paired": {
                  "type": "integer"
                },
                "only_left": {
                  "type": "integer"
                },
                "only_right": {
                  "type": "integer"
                },
                "paired_via": {
                  "type": "object",
                  "properties": {
                    "direct": {
                      "type": "integer"
                    },
                    "alias": {
                      "type": "integer"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            "counts": {
              "type": "object",
              "description": "Finding counts keyed by finding kind.",
              "additionalProperties": {
                "type": "integer"
              }
            },
            "categories": {
              "type": "object",
              "properties": {
                "breaking": {
                  "type": "integer"
                },
                "non_breaking": {
                  "type": "integer"
                },
                "compliance": {
                  "type": "integer"
                },
                "structural": {
                  "type": "integer"
                }
              },
              "additionalProperties": false
            }
          },
          "required": [
            "verdict"
          ],
          "additionalProperties": true
        },
        "operations": {
          "type": "array",
          "description": "Per-operation deltas: pairing status (paired, only_left, only_right), directional input/output schema verdicts, and findings.",
          "items": {
            "type": "object"
          }
        },
        "suppressed": {
          "type": "array",
          "description": "Findings suppressed by suppression rules, with the rule that matched.",
          "items": {
            "type": "object"
          }
        }
      },
      "required": [
        "format_version",
        "profile",
        "mode",
        "tool",
        "generated_at",
        "inputs",
        "summary",
        "operations"
      ],
      "additionalProperties": true
    },
    "ComparisonInputDescriptor": {
      "type": "object",
      "description": "Provenance of one compared document: where it came from and its canonical content hash.",
      "properties": {
        "source": {
          "type": "string",
          "description": "Locator kind (e.g. file, http, exec); empty for inline documents."
        },
        "uri": {
          "type": "string"
        },
        "content_sha256": {
          "type": "string"
        },
        "label": {
          "type": "string",
          "enum": [
            "left",
            "right"
          ]
        }
      },
      "required": [
        "source",
        "uri",
        "content_sha256",
        "label"
      ],
      "additionalProperties": false
    },
    "BindingContext": {
      "type": "object",
      "description": "Opaque runtime context for invocation, typically sourced from a context store. Well-known credential fields: bearerToken, apiKey, basic (username/password), accessToken/refreshToken/expiresAt for OAuth. Well-known transport fields: headers/cookies/environment (string-valued objects) and metadata. Implementations and callers MAY add fields; drivers SHOULD ignore fields they don't recognize.",
      "additionalProperties": true
    },
    "BindingInvocationInput": {
      "type": "object",
      "description": "Initial payload carried by the open frame: identifies the binding to invoke and provides context.",
      "properties": {
        "source": {
          "$ref": "#/schemas/Source"
        },
        "ref": {
          "type": "string",
          "description": "Reference within the binding source."
        },
        "context": {
          "$ref": "#/schemas/BindingContext"
        }
      },
      "required": [
        "source",
        "ref"
      ],
      "additionalProperties": false
    },
    "BindingInvokerInputFrame": {
      "description": "One frame from the caller into invokeBinding. The caller streams: exactly one `open` first (carrying the BindingInvocationInput), then zero or more `input` frames, then exactly one `close`.",
      "oneOf": [
        {
          "type": "object",
          "title": "open",
          "description": "Open the invocation. MUST be the first frame on the input stream.",
          "properties": {
            "kind": {
              "const": "open"
            },
            "input": {
              "$ref": "#/schemas/BindingInvocationInput"
            }
          },
          "required": [
            "kind",
            "input"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "input",
          "description": "Write one input value to the underlying binding. The value's schema is the underlying operation's, not this contract's.",
          "properties": {
            "kind": {
              "const": "input"
            },
            "value": {
              "description": "One input value, opaque at this layer."
            }
          },
          "required": [
            "kind",
            "value"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "close",
          "description": "Caller signals no more input frames will be sent. Idempotent.",
          "properties": {
            "kind": {
              "const": "close"
            }
          },
          "required": [
            "kind"
          ],
          "additionalProperties": false
        }
      ]
    },
    "BindingInvokerOutputFrame": {
      "description": "One frame from the service back to the caller. Zero or more non-terminal frames (`output`, `input_closed`) followed by exactly one terminal frame (`complete` or `error`).",
      "oneOf": [
        {
          "type": "object",
          "title": "output",
          "description": "One output value from the underlying binding. Opaque at this layer.",
          "properties": {
            "kind": {
              "const": "output"
            },
            "value": {
              "description": "One output value, opaque at this layer."
            }
          },
          "required": [
            "kind",
            "value"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "input_closed",
          "description": "The binding has closed the input side from its end. Subsequent caller input frames are ignored. Outputs may still flow.",
          "properties": {
            "kind": {
              "const": "input_closed"
            }
          },
          "required": [
            "kind"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "complete",
          "description": "Terminal: the binding completed normally. MUST be the last frame.",
          "properties": {
            "kind": {
              "const": "complete"
            }
          },
          "required": [
            "kind"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "error",
          "description": "Terminal: the binding failed. MUST be the last frame. Carries a structured InvocationError.",
          "properties": {
            "kind": {
              "const": "error"
            },
            "error": {
              "$ref": "#/schemas/InvocationError"
            }
          },
          "required": [
            "kind",
            "error"
          ],
          "additionalProperties": false
        }
      ]
    },
    "OperationInvocationInput": {
      "type": "object",
      "description": "Initial payload carried by invokeOperation's open frame: the interface to act on and which operation (or specific binding) to invoke, plus context.",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "Operation key to invoke. A binding is selected automatically."
        },
        "binding": {
          "type": "string",
          "description": "Binding key to invoke; the operation is derived from it. Mutually exclusive with operation."
        },
        "context": {
          "$ref": "#/schemas/BindingContext"
        }
      },
      "required": [
        "interface"
      ],
      "oneOf": [
        {
          "required": [
            "operation"
          ]
        },
        {
          "required": [
            "binding"
          ]
        }
      ],
      "additionalProperties": false
    },
    "OperationInvokerInputFrame": {
      "description": "One frame from the caller into invokeOperation. Same shape as the binding-invoker input stream, with the open frame carrying an OperationInvocationInput: exactly one `open` first, then zero or more `input` frames, then exactly one `close`.",
      "oneOf": [
        {
          "type": "object",
          "title": "open",
          "description": "Open the invocation. MUST be the first frame on the input stream.",
          "properties": {
            "kind": {
              "const": "open"
            },
            "input": {
              "$ref": "#/schemas/OperationInvocationInput"
            }
          },
          "required": [
            "kind",
            "input"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "input",
          "description": "Write one input value to the operation. Validated against the operation's input schema.",
          "properties": {
            "kind": {
              "const": "input"
            },
            "value": {
              "description": "One input value."
            }
          },
          "required": [
            "kind",
            "value"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "close",
          "description": "Caller signals no more input frames will be sent. Idempotent.",
          "properties": {
            "kind": {
              "const": "close"
            }
          },
          "required": [
            "kind"
          ],
          "additionalProperties": false
        }
      ]
    },
    "OperationInvokerOutputFrame": {
      "description": "One frame from invokeOperation back to the caller. Same shape as the binding-invoker output stream: zero or more `output`/`input_closed` frames, then exactly one terminal `complete` or `error`. Each output is validated against the operation's output schema.",
      "oneOf": [
        {
          "type": "object",
          "title": "output",
          "description": "One output value from the operation.",
          "properties": {
            "kind": {
              "const": "output"
            },
            "value": {
              "description": "One output value."
            }
          },
          "required": [
            "kind",
            "value"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "input_closed",
          "description": "The operation has closed the input side from its end. Subsequent caller input frames are ignored. Outputs may still flow.",
          "properties": {
            "kind": {
              "const": "input_closed"
            }
          },
          "required": [
            "kind"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "complete",
          "description": "Terminal: the operation completed normally. MUST be the last frame.",
          "properties": {
            "kind": {
              "const": "complete"
            }
          },
          "required": [
            "kind"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "title": "error",
          "description": "Terminal: the operation failed. MUST be the last frame. Carries a structured InvocationError.",
          "properties": {
            "kind": {
              "const": "error"
            },
            "error": {
              "$ref": "#/schemas/InvocationError"
            }
          },
          "required": [
            "kind",
            "error"
          ],
          "additionalProperties": false
        }
      ]
    },
    "InvocationError": {
      "type": "object",
      "description": "Structured error carried by an error output frame. Indicates terminal failure of the invocation.",
      "properties": {
        "code": {
          "type": "string",
          "description": "Machine-readable error code (e.g. ERR_CANCELLED, ERR_VALIDATION_FAILED, ERR_PROTOCOL, CONTEXT_REQUIRED)."
        },
        "message": {
          "type": "string",
          "description": "Human-readable description."
        },
        "details": {
          "description": "Optional structured details specific to the code. For CONTEXT_REQUIRED, a ContextRequiredDetails object."
        }
      },
      "required": [
        "code",
        "message"
      ],
      "additionalProperties": false
    },
    "ContextRequiredDetails": {
      "type": "object",
      "description": "The details payload of a CONTEXT_REQUIRED error, and the return shape of prepareBinding. Enumerates the context the invoker needs. `alternatives` are disjunctive (satisfy ANY one); the requirements within an alternative are conjunctive (satisfy ALL). This OR-of-AND shape captures semantics such as 'OAuth2 OR (apiKey AND clientCert)'.",
      "properties": {
        "target": {
          "type": "string",
          "description": "The target the binding addresses; the runtime derives a storage key from it."
        },
        "alternatives": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "properties": {
              "requirements": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "$ref": "#/schemas/ContextRequirement"
                }
              }
            },
            "required": [
              "requirements"
            ],
            "additionalProperties": false
          }
        }
      },
      "required": [
        "target",
        "alternatives"
      ],
      "additionalProperties": false
    },
    "ContextRequirement": {
      "type": "object",
      "description": "One prerequisite that must be satisfied before invocation, resolved into context. `type` names the requirement family (e.g. auth.bearer, auth.oauth2, auth.basic, auth.apiKey, approval.user, config.value); remaining fields are type-specific. A runtime dispatches each requirement to a resolver registered for its type.",
      "properties": {
        "type": {
          "type": "string",
          "description": "Requirement type identifier."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description shown during resolution."
        },
        "durable": {
          "type": "boolean",
          "description": "Whether resolved context MAY be cached under the challenge key and reused (true; e.g. credentials), or MUST be satisfied freshly each time (false; e.g. a one-shot approval). Defaults to true."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "Context": {
      "type": "object",
      "description": "Opaque per-target context payload: credentials, session state, headers, cookies, environment, metadata, or any other invoker-managed state. Well-known fields include bearerToken, apiKey, basic, headers, cookies, environment, metadata.",
      "additionalProperties": true
    },
    "ContextKeyInput": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "Context store key."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "SetContextInput": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "Context store key."
        },
        "value": {
          "$ref": "#/schemas/Context"
        }
      },
      "required": [
        "key",
        "value"
      ],
      "additionalProperties": false
    },
    "ContextSummary": {
      "type": "object",
      "description": "Non-secret metadata about a stored context entry. Never includes secret values.",
      "properties": {
        "url": {
          "type": "string",
          "description": "Context store key (normalized API origin or other stable identifier)."
        },
        "hasCredentials": {
          "type": "boolean"
        },
        "headerCount": {
          "type": "integer"
        },
        "cookieCount": {
          "type": "integer"
        },
        "envCount": {
          "type": "integer"
        },
        "metadataCount": {
          "type": "integer"
        },
        "loadError": {
          "type": "string",
          "description": "Set if the entry could not be parsed."
        }
      },
      "required": [
        "url",
        "hasCredentials"
      ],
      "additionalProperties": false
    },
    "RegisterDelegateInput": {
      "type": "object",
      "properties": {
        "location": {
          "type": "string",
          "description": "Delegate location: the exec: or http(s) URL of the delegate's OpenBindings interface. Also the delegate's registration identity."
        },
        "preference": {
          "type": "number",
          "description": "Initial delegate-level selection preference (higher = more preferred). May be changed later with setDelegatePreference."
        }
      },
      "required": [
        "location"
      ],
      "additionalProperties": false
    },
    "DelegateSummary": {
      "type": "object",
      "description": "A registered delegate: its identity, the snapshot of what it carries, ob's derived routing data, and its selection preferences.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name: the delegate OBI's name, or one derived from its location."
        },
        "location": {
          "type": "string",
          "description": "The reference the delegate is registered under (exec: or http(s) URL). The self-delegate's location is \"ob\"."
        },
        "operations": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Every operation identifier the delegate answers to \u2014 the keys and aliases its interface declared when last resolved. A snapshot: re-registering the location refreshes it."
        },
        "contentHash": {
          "type": "string",
          "description": "Digest of the delegate's interface document as last resolved (sha256: over its RFC 8785 canonical form). Pins the snapshot; opaque \u2014 compare for equality, don't recompute."
        },
        "capabilities": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/DelegateCapability"
          },
          "description": "Which of ob's format capabilities the delegate satisfies, checked by conformance at registration."
        },
        "formats": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/DelegateFormatInfo"
          },
          "description": "The formats the delegate reported handling when last resolved."
        },
        "preference": {
          "type": "number",
          "description": "Delegate-level selection preference \u2014 the default for every operation it carries. Higher is more preferred; absent is the baseline."
        },
        "operationPreferences": {
          "type": "object",
          "additionalProperties": {
            "type": "number"
          },
          "description": "Per-operation preference overrides, keyed by operation identifier. The override replaces the delegate-level preference when resolving that operation."
        },
        "formatPreferences": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/FormatPreference"
          },
          "description": "Preference overrides scoped to one (operation, format) pair \u2014 ob's extra granularity beyond the delegate-manager contract's per-operation index."
        },
        "builtin": {
          "type": "boolean",
          "description": "True for ob's own native handling \u2014 the self-delegate."
        }
      },
      "required": [
        "location",
        "operations"
      ],
      "additionalProperties": false
    },
    "ResolveDelegateInput": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "description": "The operation identifier a delegate is needed for."
        }
      },
      "required": [
        "operation"
      ],
      "additionalProperties": false
    },
    "ResolveDelegateResult": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "description": "The requested operation identifier, echoed back."
        },
        "candidates": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/DelegateSummary"
          },
          "description": "Registered delegates that carry the operation, ordered by effective preference, best first. Empty when none carry it."
        }
      },
      "required": [
        "operation",
        "candidates"
      ],
      "additionalProperties": false
    },
    "ResolveDelegateForFormatInput": {
      "type": "object",
      "properties": {
        "format": {
          "type": "string",
          "description": "Format token to resolve to a delegate."
        }
      },
      "required": [
        "format"
      ],
      "additionalProperties": false
    },
    "ResolveDelegateForFormatResult": {
      "type": "object",
      "properties": {
        "format": {
          "type": "string",
          "description": "The requested format token."
        },
        "name": {
          "type": "string",
          "description": "Display name of the delegate ob's routing would select."
        },
        "location": {
          "type": "string",
          "description": "The delegate's location."
        },
        "capabilities": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/DelegateCapability"
          },
          "description": "The capabilities the delegate offers for this format."
        },
        "builtin": {
          "type": "boolean",
          "description": "True when ob's own native handling is selected for the format."
        }
      },
      "required": [
        "format"
      ],
      "additionalProperties": false
    },
    "InitializeEnvironmentInput": {
      "type": "object",
      "properties": {
        "global": {
          "type": "boolean",
          "description": "Initialize the global environment (the openbindings directory under the user config dir) instead of a local .openbindings/ directory."
        }
      },
      "additionalProperties": false
    },
    "EnvironmentStatus": {
      "type": "object",
      "properties": {
        "environmentType": {
          "type": "string",
          "description": "Local or global environment."
        },
        "environmentPath": {
          "type": "string",
          "description": "Path to the .openbindings/ directory."
        },
        "delegateCount": {
          "type": "integer",
          "description": "Number of registered delegates in this environment."
        },
        "contextCount": {
          "type": "integer",
          "description": "Number of contexts in the user's context store (user-scoped, not per-environment)."
        }
      },
      "required": [
        "environmentType",
        "environmentPath"
      ],
      "additionalProperties": false
    },
    "StartServerInput": {
      "type": "object",
      "properties": {
        "port": {
          "type": "integer",
          "description": "Port to listen on (default 20290)."
        },
        "allowOrigins": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Allowed CORS origins."
        },
        "token": {
          "type": "string",
          "description": "Pre-shared session token."
        },
        "noTls": {
          "type": "boolean",
          "description": "Skip the HTTPS listener and CA trust setup (HTTP only)."
        }
      },
      "additionalProperties": false
    },
    "StartMCPServerInput": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "Interface URL to expose as an MCP server (an OBI, a well-known discovery base, or a raw binding spec). Compose an aggregate OBI to bridge several services."
        },
        "transport": {
          "type": "string",
          "enum": [
            "stdio",
            "http"
          ],
          "description": "Transport (default stdio)."
        },
        "port": {
          "type": "integer",
          "description": "HTTP port when transport is http (default 8080)."
        },
        "name": {
          "type": "string",
          "description": "MCP server name."
        }
      },
      "required": [
        "url"
      ],
      "additionalProperties": false
    },
    "ServerInfo": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "Base URL the server is listening on."
        },
        "port": {
          "type": "integer"
        }
      },
      "additionalProperties": false
    },
    "CodegenInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "language": {
          "type": "string",
          "enum": [
            "typescript",
            "go"
          ],
          "description": "Target language for the generated invoker."
        },
        "package": {
          "type": "string",
          "description": "Go package name (Go only; default derived from the interface name)."
        }
      },
      "required": [
        "interface",
        "language"
      ],
      "additionalProperties": false
    },
    "CodegenOutput": {
      "type": "object",
      "properties": {
        "language": {
          "type": "string",
          "enum": [
            "typescript",
            "go"
          ]
        },
        "code": {
          "type": "string",
          "description": "The generated source code."
        }
      },
      "required": [
        "language",
        "code"
      ],
      "additionalProperties": false
    },
    "ConformInput": {
      "type": "object",
      "description": "conform input: `interface` is the reference contract whose operations `target` must satisfy; `target` is the interface scaffolded to conform, returned modified as `interface` in the result.",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "target": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "dryRun": {
          "type": "boolean",
          "description": "Report the changes without applying them."
        }
      },
      "required": [
        "interface",
        "target"
      ],
      "additionalProperties": false
    },
    "ConformResult": {
      "type": "object",
      "description": "The conformed target document plus a report of each change made or proposed.",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "interfaceName": {
          "type": "string",
          "description": "Display name of the reference interface being conformed to."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/ConformAction"
          }
        },
        "modified": {
          "type": "boolean",
          "description": "Whether the target was changed (false under dryRun or when already conformant)."
        }
      },
      "required": [
        "interface",
        "actions",
        "modified"
      ],
      "additionalProperties": true
    },
    "ConformAction": {
      "type": "object",
      "description": "A single change conform made or would make to the target.",
      "properties": {
        "operation": {
          "type": "string",
          "description": "The operation key the change concerns."
        },
        "action": {
          "type": "string",
          "enum": [
            "scaffold",
            "replace",
            "skip",
            "compatible"
          ],
          "description": "scaffold (add a missing operation), replace (update a drifted one), skip, or compatible (already conforms)."
        },
        "details": {
          "type": "string",
          "description": "Human-readable description of the change."
        }
      },
      "required": [
        "operation",
        "action"
      ],
      "additionalProperties": false
    },
    "DemoInput": {
      "type": "object",
      "properties": {
        "httpPort": {
          "type": "integer",
          "description": "HTTP server port."
        },
        "grpcPort": {
          "type": "integer",
          "description": "gRPC server port."
        }
      },
      "additionalProperties": false
    },
    "SourceListEntry": {
      "type": "object",
      "description": "A source from the interface's sources map, with its key.",
      "properties": {
        "key": {
          "type": "string",
          "description": "The source's key in the interface's sources map."
        },
        "source": {
          "$ref": "#/schemas/Source"
        }
      },
      "required": [
        "key",
        "source"
      ],
      "additionalProperties": false
    },
    "OperationListEntry": {
      "type": "object",
      "description": "An operation from the interface's operations map, with its key and the bindings that realize it.",
      "properties": {
        "key": {
          "type": "string",
          "description": "The operation's key in the interface's operations map."
        },
        "operation": {
          "$ref": "#/schemas/Operation"
        },
        "bindings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Keys of the binding entries that realize this operation."
        }
      },
      "required": [
        "key",
        "operation"
      ],
      "additionalProperties": false
    },
    "MergeEntry": {
      "type": "object",
      "description": "One operation-level change in a merge, and whether it was applied.",
      "properties": {
        "operation": {
          "type": "string",
          "description": "The operation key the change concerns."
        },
        "action": {
          "type": "string",
          "description": "The merge action for this entry (e.g. add, update, conflict)."
        },
        "details": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "applied": {
          "type": "boolean",
          "description": "Whether this change was applied."
        }
      },
      "required": [
        "operation",
        "action",
        "applied"
      ],
      "additionalProperties": false
    },
    "DelegateLocationInput": {
      "type": "object",
      "properties": {
        "location": {
          "type": "string",
          "description": "Delegate location: the exec: or http(s) URL of the delegate's OpenBindings interface."
        }
      },
      "required": [
        "location"
      ],
      "additionalProperties": false
    },
    "DelegateCapability": {
      "type": "string",
      "enum": [
        "invoke",
        "synthesize",
        "inspect"
      ],
      "description": "One of ob's three format-handling needs, mapping to the published interface a delegate satisfies for it: invoke (binding-invoker), synthesize (interface-synthesizer), inspect (source-inspector). A capability is ob's shorthand for the operation it delegates; a delegate offers whichever subset it satisfies."
    },
    "DelegateFormatInfo": {
      "type": "object",
      "properties": {
        "format": {
          "type": "string",
          "description": "Format token the delegate handles (e.g. grpc, openapi@3.1)."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the format, when the delegate provides one."
        }
      },
      "required": [
        "format"
      ],
      "additionalProperties": false
    },
    "FormatPreference": {
      "type": "object",
      "description": "A preference override scoped to one (operation, format) pair — ob's extra granularity beyond the delegate-manager contract's per-operation index. It replaces both the delegate-level and the per-operation preference when resolving that operation for that binding-source format.",
      "properties": {
        "operation": {
          "type": "string",
          "description": "The operation identifier this override is scoped to."
        },
        "format": {
          "type": "string",
          "description": "The binding-source format this override is scoped to (e.g. grpc, openapi@3.1)."
        },
        "preference": {
          "type": "number",
          "description": "The scoped preference value; higher is more preferred."
        }
      },
      "required": [
        "operation",
        "format",
        "preference"
      ],
      "additionalProperties": false
    },
    "GetDelegateRequirementsInput": {
      "type": "object",
      "properties": {
        "capability": {
          "$ref": "#/schemas/DelegateCapability"
        }
      },
      "required": [
        "capability"
      ],
      "additionalProperties": false
    },
    "SetDelegatePreferenceInput": {
      "type": "object",
      "properties": {
        "location": {
          "type": "string",
          "description": "The reference the delegate is registered under."
        },
        "preference": {
          "type": [
            "number",
            "null"
          ],
          "description": "The new preference value; higher is more preferred, 0 is the baseline, negatives rank below it. Null clears: with an operation, it removes that operation's override; without one, it resets the delegate-level preference to the baseline."
        },
        "operation": {
          "type": "string",
          "description": "When present, the preference applies only to resolving this operation; otherwise it is the delegate-level default."
        },
        "format": {
          "type": "string",
          "description": "Optional binding-source format scope for an operation entry (requires operation) — ob's extra granularity beyond the delegate-manager contract."
        }
      },
      "required": [
        "location",
        "preference"
      ],
      "additionalProperties": false
    },
    "ProvenanceKeys": {
      "type": "object",
      "description": "Keys split by provenance: source-owned (derived from a source by pullSource) vs hand-authored.",
      "properties": {
        "sourceOwned": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "handAuthored": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "SourcePullOutput": {
      "type": "object",
      "description": "The result of a `source pull`: the resulting interface document plus the change log — the source-owned operations and bindings added, updated, or pruned, the sources pulled and skipped, and any warnings.",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "sources": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Source keys pulled."
        },
        "skipped": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Source keys skipped (hand-authored or unreadable)."
        },
        "operationsAdded": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "operationsUpdated": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "operationsPruned": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bindingsAdded": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bindingsUpdated": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bindingsPruned": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "SourceStatus": {
      "type": "object",
      "description": "Drift status of one source on an interface: what `ob source pull` would add/update/remove, plus custodial drift on hand-authored bindings whose target is gone.",
      "properties": {
        "key": {
          "type": "string"
        },
        "format": {
          "type": "string"
        },
        "ref": {
          "type": "string"
        },
        "resolve": {
          "type": "string"
        },
        "inSync": {
          "type": "boolean",
          "description": "Whether the source would change nothing and has no custodial drift."
        },
        "tracked": {
          "type": "boolean",
          "description": "Whether ob tracks this source via x-ob provenance metadata (written by addSource or synthesizeInterface). Untracked sources are hand-authored; pull skips them."
        },
        "lastSynced": {
          "type": "string"
        },
        "obVersion": {
          "type": "string"
        },
        "error": {
          "type": "string"
        },
        "operationsAdded": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "operationsUpdated": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bindingsAdded": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bindingsUpdated": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "operationsRemoved": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bindingsRemoved": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "custodial": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Hand-authored bindings whose source target no longer exists."
        }
      },
      "required": [
        "key",
        "format",
        "inSync",
        "tracked"
      ],
      "additionalProperties": false
    },
    "InterfaceStatus": {
      "type": "object",
      "description": "An interface's read-only status against its sources: per-source sync state plus the source-owned vs hand-authored breakdown of its operations and bindings.",
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "obiVersion": {
          "type": "string",
          "description": "The interface's declared OpenBindings version."
        },
        "sources": {
          "type": "array",
          "items": {
            "$ref": "#/schemas/SourceStatus"
          }
        },
        "operations": {
          "$ref": "#/schemas/ProvenanceKeys"
        },
        "bindings": {
          "$ref": "#/schemas/ProvenanceKeys"
        }
      },
      "required": [
        "obiVersion",
        "sources",
        "operations",
        "bindings"
      ],
      "additionalProperties": false
    },
    "InterfaceStatusInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "AddOperationAliasInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any existing identifier."
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "Satisfaction alias(es) to add."
        }
      },
      "required": [
        "interface",
        "operation",
        "aliases"
      ],
      "additionalProperties": false
    },
    "RemoveOperationAliasInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any existing identifier."
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "Satisfaction alias(es) to remove."
        }
      },
      "required": [
        "interface",
        "operation",
        "aliases"
      ],
      "additionalProperties": false
    },
    "ListOperationAliasesInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "Scope the satisfaction map to a single operation."
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "OperationAliasEntry": {
      "type": "object",
      "description": "An operation and the interface operations it satisfies via aliases.",
      "properties": {
        "key": {
          "type": "string"
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "key",
        "aliases"
      ],
      "additionalProperties": false
    },
    "PullSourceInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "sourceKeys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Sources to pull; empty means all registered sources."
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    },
    "SetOperationInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any identifier."
        },
        "description": {
          "type": "string"
        },
        "idempotent": {
          "type": "boolean"
        },
        "deprecated": {
          "type": "boolean"
        },
        "input": {
          "description": "Replacement JSON Schema for the operation's input."
        },
        "output": {
          "description": "Replacement JSON Schema for the operation's output."
        },
        "addTags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "removeTags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "own": {
          "type": "boolean",
          "description": "Take ownership of a source-owned operation (detach) before editing."
        }
      },
      "required": [
        "interface",
        "operation"
      ],
      "additionalProperties": false
    },
    "DetachOperationInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any identifier."
        }
      },
      "required": [
        "interface",
        "operation"
      ],
      "additionalProperties": false
    },
    "SetOperationCodegenNameInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any identifier."
        },
        "codegenName": {
          "type": "string",
          "description": "The symbol name 'ob codegen' should emit for this operation. Omit (or send empty) with clear=true to remove the override."
        },
        "clear": {
          "type": "boolean",
          "description": "Remove the codegen-name override."
        }
      },
      "required": [
        "interface",
        "operation"
      ],
      "additionalProperties": false
    },
    "SetOperationOutputSchemaInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any identifier."
        },
        "outputSchema": {
          "type": "object",
          "description": "The real output schema to elect. Omit with clear=true to remove the election."
        },
        "clear": {
          "type": "boolean",
          "description": "Remove the output-schema election."
        }
      },
      "required": [
        "interface",
        "operation"
      ],
      "additionalProperties": false
    },
    "BindOperationInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any identifier."
        },
        "source": {
          "type": "string",
          "description": "Registered source key."
        },
        "ref": {
          "type": "string",
          "description": "Source ref (wire identifier) to bind."
        },
        "preference": {
          "type": "number",
          "description": "Binding selection preference (higher = more preferred)."
        },
        "inputTransform": {
          "type": "string",
          "description": "Inline JSONata transforming operation input to binding input."
        },
        "outputTransform": {
          "type": "string",
          "description": "Inline JSONata transforming binding output to operation output."
        },
        "transformStub": {
          "type": "boolean",
          "description": "Scaffold identity transform stub(s) on shape mismatch."
        },
        "force": {
          "type": "boolean",
          "description": "Overwrite an existing binding for this operation and source."
        }
      },
      "required": [
        "interface",
        "operation",
        "source",
        "ref"
      ],
      "additionalProperties": false
    },
    "UnbindOperationInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "operation": {
          "type": "string",
          "description": "The operation's key or any identifier."
        },
        "source": {
          "type": "string",
          "description": "Source key whose binding to remove."
        }
      },
      "required": [
        "interface",
        "operation",
        "source"
      ],
      "additionalProperties": false
    },
    "NewInterfaceInput": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "openbindings": {
          "type": "string",
          "description": "Target OpenBindings spec version; defaults to the tool's max tested version."
        }
      },
      "additionalProperties": false
    },
    "SetMetadataInput": {
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/schemas/OpenBindingsInterface"
        },
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "description": {
          "type": "string"
        }
      },
      "required": [
        "interface"
      ],
      "additionalProperties": false
    }
  }
}
